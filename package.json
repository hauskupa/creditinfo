{
  "name": "webflow-js-build",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "dev": "npx esbuild src/main.js --bundle --format=iife --target=es2019 --sourcemap --outfile=dist/main.dev.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.9"
  }
}
